# 本文件夹存放项目的**核心源代码**。

## 建议结构

```
src/
├── models/        ← 模型定义（网络结构、模块等）
├── utils/         ← 工具函数（数据处理、日志、计算指标等）
└── __init__.py
```

## 填写说明

请在此描述各模块的作用，例如：

- `models/` — 包含 XXX 模型的定义，核心类为 `XXXModel`
- `utils/` — 包含数据预处理工具函数和评测指标计算
